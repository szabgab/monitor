

## Release

* Commit all the changes
* Update the README.md with the new tag
* Update this file with the new tag
* Commit all the changes

```
$ git push
$ git tag -a v1.03 -m v1.03
$ git push --tags
```

